<% meta title: t('pages.search_title'),
        robots: 'noindex' %>
<% params[:search] = nil %>

<h1 class="text-center"><%= t('pages.search_title') %></h1>

<%= render @organizations %>

<div class="col-xs-12 text-center"><%= paginate @organizations %></div>
